import { ShowOnContextDirective } from './show-on-context.directive';

describe('ShowOnContextDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOnContextDirective();
    expect(directive).toBeTruthy();
  });
});
